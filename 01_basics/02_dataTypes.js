"use strict"; // This is to treat all the code in this file as per the new version

let name = "Abhilash";
let age = 29;
let isLoggedIn = false;


console.log(typeof undefined); // undefined
console.log(typeof null); // object, null object

/**
 * number, bigInt -  to store numeric data
 * string - list of char
 * boolean - true / false, 
 * 
 * null -> data type as well as a standalone value. represents empty value. null can be assigned to a variable
 * undefined -> when value is not defined.
 * symbol -> used mainly for uniqueness
 * 
 * object -> 
 */